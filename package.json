{
  "name": "agently-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Supabase-backed backend server for the Agently SaaS app.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
